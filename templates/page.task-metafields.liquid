<style>
  .products {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-gap: 20px;
  }
</style>
<h1>{{ page.metafields.pageSection.title }}</h1>
<div class="products">
  {% render 'metafield-product', products_array: page.metafields.pageSection.productItem %}
</div>
